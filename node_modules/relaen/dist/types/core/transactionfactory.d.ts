import { Connection } from "./connection";
import { Transaction } from "./transaction";
/**
 * 事务类型工厂
 */
export declare class TransactionFactory {
    /**
     * 事务类型集合
     */
    private static transactions;
    /**
     * 事务map集合
     */
    private static classMap;
    /**
     * 添加事务类
     * @param name -    dialect 名
     * @param value -   transaction 类
     */
    static addClass(name: string, value: unknown): void;
    /**
     * 获取事务类
     * @param conn -    连接对象，如果不设置，当不存在事务对象时，则不进行新建
     * @returns         事务对象
     */
    static get(conn?: Connection): Transaction;
    /**
     * 删除事务
     * @param threadId -  thread id，默认为当前thread id
     */
    static remove(threadId?: number): void;
}
